name: analytics_platform
version: "1.0.0"
config-version: 2
profile: analytics_platform

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  analytics_platform:
    staging:
      +materialized: view
    marts:
      +materialized: table
